using System;
using Splat;

namespace WorkoutLog
{
    public class Module
    {
        public static void Init()
        {
            //Locator.CurrentMutable.Register(() => new );
        }
    }
}
